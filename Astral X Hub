-- ===========================================
-- ASTRAL X HUB - ESTRUTURA COMPLETA
-- Apenas interface, sem nenhuma função ativa
-- ===========================================
local WindUI = loadstring(game:HttpGet(
"https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"
))()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- =========================
-- VARIÁVEIS DA ABA DIVERSÃO 
-- =========================
local walkSpeed = 16
local jumpPower = 50
local infJump = false
local noclip = false
local fly = false
local flySpeed = 50
local selectedPlayer = nil
local touchKick = false
local flingAll = false

-- =========================
-- JANELA PRINCIPAL
-- =========================
local Window = WindUI:CreateWindow({
Title = "ASTRAL X Hub",
Folder = "AstralX",
Size = UDim2.fromOffset(580,460),
MinSize = Vector2.new(560,350),
MaxSize = Vector2.new(850,560),
Transparent = true,
Theme = "Dark",
Resizable = true,
SideBarWidth = 200,
BackgroundImageTransparency = 0.42,
HideSearchBar = true,
ScrollBarEnabled = false,
})

-- =========================
-- ABA ASSASSINO
-- =========================
local Aba_Assassino = Window:Tab({
Title="Assassino"
})
-- ✅ COLOQUE AS FUNÇÕES DAQUI DEPOIS




-- =========================
-- ABA XERIFE
-- =========================
local Aba_Xerife = Window:Tab({
Title="Xerife"
})
-- ✅ COLOQUE AS FUNÇÕES DAQUI DEPOIS




-- =========================
-- ABA ESP
-- =========================
local Aba_ESP = Window:Tab({
Title="ESP"
})
-- ✅ COLOQUE AS FUNÇÕES DAQUI DEPOIS




-- =========================
-- ABA FARM
-- =========================
local Aba_Farm = Window:Tab({
Title="Farm"
})
-- ✅ COLOQUE AS FUNÇÕES DAQUI DEPOIS


-- =========================
-- ABA DIVERSÃO
-- =========================
local Aba_Diversao = Window:Tab({
Title="Diversão"
})
-- ✅ COLOQUE AS FUNÇÕES DAQUI DEPOIS

local Sec_Jogador = Aba_Diversao:Section({Title="Controle do Jogador"})

Sec_Jogador:Slider({
Title="Velocidade de Andar",
Value={Min=16, Max=250, Default=16},
Callback=function(v) walkSpeed = v end
})

Sec_Jogador:Slider({
Title="Força do Pulo",
Value={Min=50, Max=250, Default=50},
Callback=function(v) jumpPower = v end
})

Sec_Jogador:Toggle({
Title="Pulo Infinito",
Default=false,
Callback=function(s) infJump = s end
})

Sec_Jogador:Toggle({
Title="Atravessar Paredes (Noclip)",
Default=false,
Callback=function(s) noclip = s end
})

Sec_Jogador:Toggle({
Title="Modo Voo",
Default=false,
Callback=function(s) fly = s end
})

Sec_Jogador:Slider({
Title="Velocidade do Voo",
Value={Min=20, Max=300, Default=50},
Callback=function(v) flySpeed = v end
})

Sec_Jogador:Section({Title="Sistema de Empurrar"})

Sec_Jogador:Dropdown({
Title = "Escolher Jogador",
Values = {"Carregando..."},
Callback = function(v)
selectedPlayer = v and Players:FindFirstChild(v) or nil
end
})

Sec_Jogador:Button({
Title="Empurrar Xerife",
Callback=function()
local sheriff = findSheriff()
if sheriff then walkFling(sheriff) end
end
})

Sec_Jogador:Button({
Title="Empurrar Assassino",
Callback=function()
local murderer = findMurderer()
if murderer then walkFling(murderer) end
end
})

Sec_Jogador:Button({
Title="Empurrar Selecionado",
Callback=function()
if selectedPlayer then walkFling(selectedPlayer) end
end
})

Sec_Jogador:Toggle({
Title="Empurrar ao Tocar",
Default=false,
Callback=function(s) touchKick = s end
})

Sec_Jogador:Toggle({
Title="Empurrar Todos",
Default=false,
Callback=function(s) flingAll = s end
})

-- =========================
-- ABA TELEPORTE
-- =========================
local Aba_Teleporte = Window:Tab({
Title="Teleporte"
})
-- ✅ COLOQUE AS FUNÇÕES DAQUI DEPOIS

-- ==========================
-- FUNÇÕES GERAIS
-- ==========================

-- SISTEMA MOVIMENTO, NOCLIP E VOO
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

UIS.JumpRequest:Connect(function()
    if infJump and player.Character then
        local hum = player.Character:FindFirstChild("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

UIS.InputChanged:Connect(function(entrada, processado)
    if processado or not fly then return end
    local char = player.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not root or not hum then return end

    local dir = Vector3.new()
    if UIS:IsKeyDown(Enum.KeyCode.W) then dir += workspace.CurrentCamera.CFrame.LookVector end
    if UIS:IsKeyDown(Enum.KeyCode.S) then dir -= workspace.CurrentCamera.CFrame.LookVector end
    if UIS:IsKeyDown(Enum.KeyCode.A) then dir -= workspace.CurrentCamera.CFrame.RightVector end
    if UIS:IsKeyDown(Enum.KeyCode.D) then dir += workspace.CurrentCamera.CFrame.RightVector end
    if UIS:IsKeyDown(Enum.KeyCode.Space) then dir += Vector3.new(0,1,0) end
    if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then dir += Vector3.new(0,-1,0) end

    if dir.Magnitude > 0 then dir = dir.Unit end
    root.Velocity = dir * flySpeed
    hum.PlatformStand = true
end)

RunService.Stepped:Connect(function()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    if not hum then return end

    hum.WalkSpeed = walkSpeed
    hum.JumpPower = jumpPower

    if noclip then
        for _,p in pairs(char:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide = false end
        end
    else
        for _,p in pairs(char:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide = true end
        end
    end

    if fly then
        hum.PlatformStand = true
    end
end)

-- =========================
-- SISTEMA FLING / EMPURRAR
-- =========================
local function hasItem(plr,item)
local bp = plr:FindFirstChild("Backpack")
local ch = plr.Character
return (bp and bp:FindFirstChild(item)) or (ch and ch:FindFirstChild(item))
end

local function findSheriff()
for _,v in pairs(Players:GetPlayers()) do if v~=player and hasItem(v,"Gun") then return v end end
end

local function findMurderer()
for _,v in pairs(Players:GetPlayers()) do if v~=player and hasItem(v,"Knife") then return v end end
end

local function walkFling(target)
local char = player.Character
if not char then return end
local root = char:FindFirstChild("HumanoidRootPart")
local hum = char:FindFirstChild("Humanoid")
if not root or not hum or not target.Character then return end
local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
if not targetRoot then return end

local oldPos = root.CFrame
local cam = workspace.CurrentCamera
local oldCam = cam.CameraSubject
local targetHum = target.Character:FindFirstChild("Humanoid")
if targetHum then cam.CameraSubject = targetHum end

local spin = Instance.new("BodyAngularVelocity")
spin.MaxTorque = Vector3.new(math.huge,math.huge,math.huge)
spin.AngularVelocity = Vector3.new(0,99999,0)
spin.P = math.huge
spin.Parent = root

task.spawn(function()
for i=1, 60 do
RunService.Heartbeat:Wait()
local vel = root.Velocity
root.Velocity = vel * 8000 + Vector3.new(0,8000,0)
RunService.RenderStepped:Wait()
root.Velocity = vel
end
end)

local inicio = tick()
while tick() - inicio < 2 do
if targetRoot then
root.CFrame = targetRoot.CFrame * CFrame.new(math.random(-2,2),0,math.random(-2,2))
root.Velocity = (targetRoot.Position - root.Position).Unit * 120
end
RunService.Heartbeat:Wait()
end

spin:Destroy()
root.Velocity = Vector3.zero
root.RotVelocity = Vector3.zero
cam.CameraSubject = oldCam
root.CFrame = oldPos
end

-- LOOP DO SISTEMA DE EMPURRAR
RunService.Heartbeat:Connect(function()
-- Empurrar ao tocar
if touchKick then
local char = player.Character
if not char then return end
local root = char:FindFirstChild("HumanoidRootPart")
if not root then return end
for _,plr in pairs(Players:GetPlayers()) do
if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
local dist = (root.Position - plr.Character.HumanoidRootPart.Position).Magnitude
if dist < 3 then walkFling(plr) end
end
end
end

-- Empurrar todos
if flingAll then
task.spawn(function()
for _,plr in pairs(Players:GetPlayers()) do
if not flingAll then break end
if plr ~= player then walkFling(plr) task.wait(1) end
end
end)
end
end)
