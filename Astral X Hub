-- ===========================================
-- ASTRAL X HUB · COMPLETO E ARRUMADO
-- ✅ PROTEÇÃO: NÃO ABRE 2 JANELAS
-- ✅ HITBOX NA ABA ASSASSINO
-- ✅ ANTI-AFK NA ABA FARM
-- ✅ AUTO FARM COM TODAS AS VERIFICAÇÕES
-- ✅ TUDO FUNCIONANDO
-- ===========================================

local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local player = Players.LocalPlayer

-- ✅ PROTEÇÃO CONTRA DUPLICATA
local NomeJanela = "ASTRAL X Hub"
if game.CoreGui:FindFirstChild(NomeJanela) then game.CoreGui[NomeJanela]:Destroy() end
if player.PlayerGui:FindFirstChild(NomeJanela) then player.PlayerGui[NomeJanela]:Destroy() end

-- ===========================================
-- FUNÇÕES AUXILIARES
-- ===========================================
local function HRP()
    local char = player.Character or player.CharacterAdded:Wait()
    return char:WaitForChild("HumanoidRootPart")
end

local function GetMapa()
    for _,v in ipairs(workspace:GetChildren()) do
        if v:IsA("Model") then
            if v:FindFirstChild("Spawns", true) and v.Name ~= "Lobby" and not string.find(v.Name:lower(),"lobby") then
                return v
            end
        end
    end
end

local function GetLobby()
    for _,v in ipairs(workspace:GetChildren()) do
        if v:IsA("Model") then
            local n = v.Name:lower()
            if string.find(n,"lobby") then return v end
        end
    end
end

local function GetBasePart(model)
    if not model then return end
    if model.PrimaryPart then return model.PrimaryPart end
    return model:FindFirstChildWhichIsA("BasePart", true)
end

local function IrPartida()
    local mapa = GetMapa()
    if mapa then
        local spawn = mapa:FindFirstChild("Spawns", true) or mapa:FindFirstChild("PlayerSpawns", true)
        if spawn then
            local part = spawn:FindFirstChildWhichIsA("BasePart", true)
            if part then HRP().CFrame = part.CFrame + Vector3.new(0,5,0) return end
        end
        local part = GetBasePart(mapa)
        if part then HRP().CFrame = part.CFrame + Vector3.new(0,5,0) return end
    end
    for _,plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            HRP().CFrame = plr.Character.HumanoidRootPart.CFrame + Vector3.new(0,5,3)
            return
        end
    end
end

local function IrLobby()
    local lobby = GetLobby()
    if lobby then
        local part = GetBasePart(lobby)
        if part then HRP().CFrame = part.CFrame + Vector3.new(0,5,0) end
    end
end

-- ===========================================
-- VARIÁVEIS GERAIS
-- ===========================================
local walkSpeed = 16
local jumpPower = 50
local infJump = false

local ESP_ATIVO = false
local ESP_Arma = true
local UltimoCargos = {}
local Murder, Sheriff, Hero = nil, nil, nil

local ZoomInfinito = false
local CameraAtravessaParede = false
local FOV_Atual = 70
local ZoomMin_Padrao = 0.5
local ZoomMax_Padrao = 128

local CONFIG_FARM = {velocidade = 25, limite = 40, alcance_maximo = 999}
local AutoFarmAtivo = false
local collectedCoins = {}

-- ===========================================
-- INTERFACE PRINCIPAL
-- ===========================================
local Window = WindUI:CreateWindow({
    Title = "ASTRAL X Hub",
    Folder = "AstralX",
    Size = UDim2.fromOffset(580,460),
    MinSize = Vector2.new(560,350),
    MaxSize = Vector2.new(850,560),
    Transparent = true,
    Theme = "Dark",
    Resizable = true,
    SideBarWidth = 200,
    BackgroundImageTransparency = 0.42,
    HideSearchBar = true,
    ScrollBarEnabled = false,
})

-- =========================
-- ABA ASSASSINO
-- =========================
local Aba_Assassino = Window:Tab({Title="Assassino"})
local Sec_Hitbox = Aba_Assassino:Section({Title="Hitbox"})

local HitboxAtivo = false
local TamanhoHit = 12

Sec_Hitbox:Toggle({
    Title = "Ativar Hitbox",
    Default = false,
    Callback = function(s) HitboxAtivo = s end
})

Sec_Hitbox:Slider({
    Title = "Tamanho (5 a 50)",
    Value = {Min=5, Max=50, Default=12},
    Callback = function(v) TamanhoHit = v end
})

-- SISTEMA DA HITBOX
RunService.Stepped:Connect(function()
    local c = player.Character
    if not c then return end
    local faca = c:FindFirstChild("Knife")
    if not faca then return end

    if HitboxAtivo then
        for _, p in ipairs(faca:GetChildren()) do
            if p:IsA("BasePart") then
                p.Size = Vector3.new(TamanhoHit, TamanhoHit, TamanhoHit)
                p.CanCollide = false
            end
        end
    else
        for _, p in ipairs(faca:GetChildren()) do
            if p:IsA("BasePart") then
                p.Size = Vector3.new(1.8, 1, 5.5)
            end
        end
    end
end)

-- =========================
-- ABA XERIFE · SILENT AIM + AIMBOT COMPLETO
-- =========================
local Aba_Xerife = Window:Tab({Title="Xerife"})
local Sec_Silent = Aba_Xerife:Section({Title="Mira Automática"})

-- VARIAVEIS DO SISTEMA
local silentAimEnabled = false
local aimbotEnabled = false
local aimbotFOV = 100
local aimbotSmoothing = 5
local aimbotActive = false

-- FUNÇÕES AUXILIARES DA MIRA
local function getRole(p)
    local c = p.Character
    if not c then return nil end
    local bk = p:FindFirstChild("Backpack")
    if c:FindFirstChild("Knife") or (bk and bk:FindFirstChild("Knife")) then return "Murderer" end
    if c:FindFirstChild("Gun") or (bk and bk:FindFirstChild("Gun")) then return "Sheriff" end
    return "Innocent"
end

local function getClosestPlayerToCursor(fov, filtro)
    local cam = Workspace.CurrentCamera
    local mouse = UIS:GetMouseLocation()
    local alvo, dist = nil, fov
    for _,j in pairs(Players:GetPlayers()) do
        if j~=player and j.Character then
            if filtro == "Murderer" and getRole(j)~="Murderer" then goto continue end
            local hrp = j.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local pos, vis = cam:WorldToViewportPoint(hrp.Position)
                if vis then
                    local d = math.sqrt((pos.X-mouse.X)^2 + (pos.Y-mouse.Y)^2)
                    if d < dist then dist=d alvo=j end
                end
            end
            ::continue::
        end
    end
    return alvo
end

local function smoothAim(atual, alvo, valor)
    return atual:Lerp(alvo, math.clamp(valor/10, 0.05, 0.5))
end

-- INTERFACE DA ABA
Sec_Silent:Toggle({
    Title = "🔇 Silent Aim",
    Default = false,
    Callback = function(s)
        silentAimEnabled = s
        WindUI:Notify({Title="Xerife", Content=s and "ATIVADO" or "DESATIVADO", Duration=1.5})
    end
})

Sec_Silent:Toggle({
    Title = "🎯 Aimbot (Segurar Botão Direito)",
    Default = false,
    Callback = function(s)
        aimbotEnabled = s
        WindUI:Notify({Title="Xerife", Content=s and "ATIVADO" or "DESATIVADO", Duration=1.5})
    end
})

Sec_Silent:Slider({
    Title = "Campo de Visão (FOV)",
    Value = {Min=30, Max=360, Default=100},
    Callback = function(v) aimbotFOV = v end
})

Sec_Silent:Slider({
    Title = "Suavidade",
    Value = {Min=0, Max=10, Default=5},
    Callback = function(v) aimbotSmoothing = v end
})

-- SISTEMA SILENT AIM
task.spawn(function()
    while task.wait(0.05) do
        if not silentAimEnabled then continue end
        local c = player.Character
        if not c then continue end
        local gun = c:FindFirstChild("Gun")
        if not gun then continue end
        local alvo = getClosestPlayerToCursor(aimbotFOV, "Murderer")
        if alvo and alvo.Character then
            local hrp = alvo.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                pcall(function()
                    local rem = gun:FindFirstChild("Remote")
                    if rem and not gun._orig then
                        gun._orig = rem.FireServer
                        rem.FireServer = function(self, ...)
                            gun._orig(self, hrp.Position, ...)
                        end
                    end
                end)
            end
        end
    end
end)

-- SISTEMA AIMBOT
UIS.InputBegan:Connect(function(i, proc)
    if proc then return end
    aimbotActive = (i.UserInputType == Enum.UserInputType.MouseButton2)
end)

UIS.InputEnded:Connect(function(i, proc)
    if proc then return end
    aimbotActive = false
end)

RunService.RenderStepped:Connect(function()
    if not aimbotEnabled or not aimbotActive then return end
    local c = player.Character
    if not c or not c:FindFirstChild("Gun") then return end
    local alvo = getClosestPlayerToCursor(aimbotFOV, "Murderer")
    if alvo and alvo.Character then
        local hrp = alvo.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local cam = Workspace.CurrentCamera
            local nova = CFrame.new(cam.CFrame.Position, hrp.Position)
            cam.CFrame = aimbotSmoothing>0 and smoothAim(cam.CFrame, nova, aimbotSmoothing) or nova
        end
    end
end)

-- =========================
-- ABA ESP
-- =========================
local Aba_ESP = Window:Tab({Title="ESP"})
local Sec_ESP = Aba_ESP:Section({Title = "Configurações do ESP"})

Sec_ESP:Toggle({Title = "Mostrar Jogadores", Default = false, Callback = function(s) ESP_ATIVO = s end})
Sec_ESP:Toggle({Title = "Marcar Armas", Default = true, Callback = function(s) ESP_Arma = s end})

-- =========================
-- ABA FARM
-- =========================
local Aba_Farm = Window:Tab({Title="Farm"})
local Sec_Farm = Aba_Farm:Section({Title = "Auto Farm MM2"})

Sec_Farm:Toggle({
    Title = "✅ Ativar Auto Farm",
    Default = false,
    Callback = function(estado)
        AutoFarmAtivo = estado
        WindUI:Notify({
            Title = "Auto Farm",
            Content = estado and "ATIVADO COM SUCESSO!" or "DESATIVADO!",
            Duration = 2
        })
    end
})

Sec_Farm:Button({
    Title = "🛡️ Ativar Anti-AFK",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/hassanxzayn-lua/Anti-afk/main/antiafkbyhassanxzyn", true))()
        end)
    end
})

-- =========================
-- ABA JOGADOR
-- =========================
local Aba_Diversao = Window:Tab({Title="Jogador"})
local Sec_Jogador = Aba_Diversao:Section({Title="Controle do Jogador"})

Sec_Jogador:Slider({Title="Velocidade de Andar", Value={Min=16, Max=250, Default=16}, Callback=function(v) walkSpeed = v end})
Sec_Jogador:Slider({Title="Força do Pulo", Value={Min=50, Max=250, Default=50}, Callback=function(v) jumpPower = v end})
Sec_Jogador:Toggle({Title="Pulo Infinito", Default=false, Callback=function(s) infJump = s end})

-- ===========================================
-- 🎭 SESSÃO: SCRIPT DE EMOÇÕES NÃO OFICIAL
-- ===========================================
local Sec_Emotes_NaoOficial = Aba_Diversao:Section({Title="Script de Emoções Não Oficial"})

-- BOTÃO QUE EXECUTA O SEU SCRIPT
Sec_Emotes_NaoOficial:Button({
    Title = "▶️ Executar Script",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Bac0nHck/Scripts/refs/heads/main/BundleAnimations.lua"))()
        end)
    end
})

-- =========================
-- ABA TELEPORTE
-- =========================
local Aba_Teleporte = Window:Tab({Title="Teleporte"})
local Sec_Tele = Aba_Teleporte:Section({Title = "MM2"})
Sec_Tele:Button({Title = "Ir para o Lobby", Callback = IrLobby})
Sec_Tele:Button({Title = "Ir para a Partida", Callback = IrPartida})

-- ===========================================
-- SISTEMA ESP COMPLETO
-- ===========================================
local function CriarBrilho(Jogador)
    if not Jogador or not Jogador.Character then return end
    if not Jogador.Character:FindFirstChild("Highlight") then
        local hl = Instance.new("Highlight")
        hl.Name = "Highlight"
        hl.Adornee = Jogador.Character
        hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        hl.OutlineTransparency = 0
        hl.Enabled = false
        hl.Parent = Jogador.Character
    end
end

local function EstaVivo(Jogador, listaCargos)
    if not listaCargos or not listaCargos[Jogador.Name] then return false end
    local Info = listaCargos[Jogador.Name]
    return not Info.Killed and not Info.Dead
end

task.spawn(function()
    while true do
        if ESP_ATIVO then
            local PegarDados = ReplicatedStorage:FindFirstChild("GetPlayerData", true)
            if PegarDados then
                local ok, res = pcall(function() return PegarDados:InvokeServer() end)
                if ok and type(res) == "table" then
                    local novoMurder, novoSheriff, novoHero = nil, nil, nil
                    for Nome,DadosJogador in pairs(res) do
                        if DadosJogador.Role == "Murderer" then novoMurder = Nome
                        elseif DadosJogador.Role == "Sheriff" then novoSheriff = Nome
                        elseif DadosJogador.Role == "Hero" then novoHero = Nome end
                    end
                    if novoMurder then Murder = novoMurder end
                    if novoSheriff then Sheriff = novoSheriff end
                    if novoHero then Hero = novoHero end
                    UltimoCargos = res
                end
            end
        end
        task.wait(0.3)
    end
end)

task.spawn(function()
    while true do
        if not ESP_ATIVO then
            for _,J in pairs(Players:GetPlayers()) do
                if J.Character and J.Character:FindFirstChild("Highlight") then
                    J.Character.Highlight.Enabled = false
                end
            end
        else
            for _,J in pairs(Players:GetPlayers()) do
                if J == player or not J.Character then continue end
                CriarBrilho(J)
                local hl = J.Character:FindFirstChild("Highlight")
                if not hl then continue end

                local XerifeMorto = Sheriff and Players:FindFirstChild(Sheriff) and not EstaVivo(Players[Sheriff], UltimoCargos) or false

                if J.Name == Murder and EstaVivo(J, UltimoCargos) then
                    hl.FillColor = Color3.fromRGB(255, 40, 40)
                elseif J.Name == Sheriff and EstaVivo(J, UltimoCargos) then
                    hl.FillColor = Color3.fromRGB(40, 120, 255)
                elseif J.Name == Hero and EstaVivo(J, UltimoCargos) and XerifeMorto then
                    hl.FillColor = Color3.fromRGB(255, 215, 0)
                else
                    hl.FillColor = Color3.fromRGB(50, 255, 50)
                end
                hl.OutlineColor = hl.FillColor
                hl.Enabled = true
            end
        end
        task.wait(0.15)
    end
end)

-- ESP ARMAS
local MarcaArmas = {}
task.spawn(function()
    while true do
        if not ESP_ATIVO or not ESP_Arma then
            for _,dados in pairs(MarcaArmas) do if dados.Destaque then dados.Destaque.Enabled = false end end
            task.wait(0.3) continue
        end
        for _,obj in ipairs(Workspace:GetDescendants()) do
            if obj.Name ~= "GunDrop" then continue end
            local caminho = obj:GetFullName():lower()
            if caminho:find("lobby") or caminho:find("weapondisplays") then continue end
            if obj:FindFirstAncestorWhichIsA("Humanoid") or obj:FindFirstAncestorWhichIsA("Backpack") then continue end

            local Parte = obj:IsA("BasePart") and obj or obj:FindFirstChildWhichIsA("BasePart", true)
            if not Parte then continue end

            if not MarcaArmas[obj] then
                local Destaque = Instance.new("Highlight")
                Destaque.Adornee = obj
                Destaque.FillTransparency = 1
                Destaque.OutlineTransparency = 0
                Destaque.OutlineColor = Color3.fromRGB(255,255,0)
                Destaque.Parent = game.CoreGui

                local Texto = Instance.new("BillboardGui")
                Texto.Adornee = Parte
                Texto.Size = UDim2.new(0,65,0,22)
                Texto.StudsOffset = Vector3.new(0,2.2,0)
                Texto.AlwaysOnTop = true
                Texto.Parent = Parte

                local Label = Instance.new("TextLabel")
                Label.Size = UDim2.fromScale(1,1)
                Label.BackgroundTransparency = 1
                Label.Text = "ARMA"
                Label.TextColor3 = Color3.fromRGB(255,255,0)
                Label.TextScaled = true
                Label.Font = Enum.Font.GothamBold
                Label.Parent = Texto

                MarcaArmas[obj] = {Destaque=Destaque, Texto=Texto}
            end
            MarcaArmas[obj].Destaque.Enabled = true
            MarcaArmas[obj].Texto.Enabled = true
        end
        for obj,_ in pairs(MarcaArmas) do
            if not obj or not obj.Parent then MarcaArmas[obj] = nil end
        end
        task.wait(0.3)
    end
end)

-- ===========================================
-- AUTO FARM COM TODAS AS VERIFICAÇÕES
-- ===========================================
local function EmPartida()
    local char = player.Character
    if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then return false end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player and p.Character and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
            return true
        end
    end
    return false
end

local function QuantidadeMochila()
    local ok, n = pcall(function()
        local g = player:WaitForChild("PlayerGui"):WaitForChild("MainGUI", 2)
        return tonumber(g.Game.CoinBags.Container.SnowToken.CurrencyFrame.Icon.Coins.Text) or 0
    end)
    return ok and n or 0
end

local function MoedaValida(moeda)
    return moeda and moeda.Parent 
    and moeda:IsA("BasePart")
    and moeda:FindFirstChild("TouchInterest")
    and moeda:FindFirstChild("CoinVisual")
    and not moeda:GetAttribute("Collected")
end

local function getNearestCoin(minhaPos)
    local melhorMoeda = nil
    local menorDist = math.huge
    local CoinContainer = Workspace:FindFirstChild("CoinContainer", true)
    if not CoinContainer then return nil end

    for _, v in ipairs(CoinContainer:GetChildren()) do  
        if v:IsA("BasePart")  
        and v.Name == "Coin_Server"  
        and v:FindFirstChild("TouchInterest")  
        and v:FindFirstChild("CoinVisual")  
        and not collectedCoins[v]  
        and not v:GetAttribute("Collected") then  

            local dist = (minhaPos - v.Position).Magnitude  
            if dist < CONFIG_FARM.alcance_maximo and dist < menorDist then  
                menorDist = dist  
                melhorMoeda = v  
            end  
        end  
    end  
    return melhorMoeda
end

task.spawn(function()
    while true do
        if AutoFarmAtivo then
            if not EmPartida() then
                table.clear(collectedCoins)
                RunService.Heartbeat:Wait()
                continue
            end

            local atual = QuantidadeMochila()
            if atual >= CONFIG_FARM.limite then
                repeat
                    RunService.Heartbeat:Wait()
                    atual = QuantidadeMochila()
                until atual < CONFIG_FARM.limite or not AutoFarmAtivo or not EmPartida()
                table.clear(collectedCoins)
                continue
            end

            local c = player.Character
            if not c or not c.PrimaryPart then RunService.Heartbeat:Wait() continue end

            local minhaPos = c.PrimaryPart.Position
            local alvo = getNearestCoin(minhaPos)

            if alvo then
                local ini = minhaPos
                local destino = alvo.Position
                local distTotal = (ini - destino).Magnitude
                local tempo = distTotal / CONFIG_FARM.velocidade
                local tIni = os.clock()

                while AutoFarmAtivo and EmPartida() and MoedaValida(alvo) and QuantidadeMochila() < CONFIG_FARM.limite do
                    local char = player.Character
                    if not char or not char.PrimaryPart then break end

                    local novaPos = char.PrimaryPart.Position
                    local novoAlvo = getNearestCoin(novaPos)
                    if novoAlvo and novoAlvo ~= alvo then
                        alvo = novoAlvo
                        ini = novaPos
                        destino = alvo.Position
                        distTotal = (ini - destino).Magnitude
                        tempo = distTotal / CONFIG_FARM.velocidade
                        tIni = os.clock()
                    end

                    local progresso = math.min((os.clock() - tIni) / tempo, 1)
                    char:PivotTo(CFrame.new(ini:Lerp(destino, progresso)))

                    if not MoedaValida(alvo) then break end
                    if progresso >= 1 then break end

                    RunService.Heartbeat:Wait()
                end

                if alvo then collectedCoins[alvo] = true end
            else
                table.clear(collectedCoins)
                RunService.Heartbeat:Wait()
            end
        end
        RunService.Heartbeat:Wait()
    end
end)

-- MOVIMENTO GERAL
UIS.JumpRequest:Connect(function()
    if infJump and player.Character then
        local h = player.Character:FindFirstChild("Humanoid")
        if h then h:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

RunService.Stepped:Connect(function()
    local c = player.Character
    if c then
        local h = c:FindFirstChild("Humanoid")
        if h then
            h.WalkSpeed = walkSpeed
            h.JumpPower = jumpPower
        end
    end
end)
