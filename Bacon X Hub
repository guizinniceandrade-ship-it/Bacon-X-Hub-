-- ===========================================
-- Astral X Hub - Interface (LocalScript)
-- ===========================================
-- Cole este script em um LocalScript dentro de StarterPlayer > StarterPlayerScripts
-- (ou execute via um executor, no caso de testes)

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- =========================
-- VERIFICAÃ‡ÃƒO DE JOGO
-- =========================
local FLEE_THE_FACILITY_PLACE_ID = 893973440

if game.PlaceId ~= FLEE_THE_FACILITY_PLACE_ID then
	game:GetService("StarterGui"):SetCore("SendNotification", {
		Title = "Astral X Hub",
		Text = "Entre no jogo correto (Flee the Facility) para poder ser executado",
		Duration = 6,
	})
	return
end

-- Remove instÃ¢ncia antiga, se existir
if playerGui:FindFirstChild("AstralXHub") then
	playerGui.AstralXHub:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AstralXHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = playerGui

-- =========================
-- BOTÃƒO (abre/fecha o hub)
-- =========================
local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Size = UDim2.new(0, 50, 0, 50)
ToggleButton.Position = UDim2.new(0, 20, 0, 20)
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleButton.BorderSizePixel = 0
ToggleButton.AutoButtonColor = false
ToggleButton.Text = ""
ToggleButton.Parent = ScreenGui

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 12)
btnCorner.Parent = ToggleButton

local btnStroke = Instance.new("UIStroke")
btnStroke.Color = Color3.fromRGB(255, 255, 255)
btnStroke.Thickness = 2
btnStroke.Parent = ToggleButton

local btnIcon = Instance.new("ImageLabel")
btnIcon.Size = UDim2.new(0, 40, 0, 40)
btnIcon.Position = UDim2.new(0.5, -20, 0.5, -20)
btnIcon.BackgroundTransparency = 1
btnIcon.Image = "rbxassetid://133983783420812"
btnIcon.ZIndex = 2
btnIcon.Parent = ToggleButton

-- Permite mover o botÃ£o livremente pela tela
ToggleButton.Active = true
ToggleButton.Draggable = true

-- =========================
-- PAINEL PRINCIPAL (550x350)
-- =========================
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 550, 0, 350)
MainFrame.Position = UDim2.new(0.5, -275, 0.5, -175)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 14)
frameCorner.Parent = MainFrame

local frameStroke = Instance.new("UIStroke")
frameStroke.Color = Color3.fromRGB(255, 255, 255)
frameStroke.Thickness = 2
frameStroke.Parent = MainFrame

-- Permite mover o painel livremente pela tela
MainFrame.Active = true
MainFrame.Draggable = true

-- =========================
-- FUNDO DE ESTRELAS ANIMADAS
-- =========================
local StarsContainer = Instance.new("Frame")
StarsContainer.Name = "StarsContainer"
StarsContainer.Size = UDim2.new(1, 0, 1, 0)
StarsContainer.BackgroundTransparency = 1
StarsContainer.ZIndex = 1
StarsContainer.Parent = MainFrame

local stars = {}
for i = 1, 50 do
	local star = Instance.new("Frame")
	star.Size = UDim2.new(0, math.random(1, 2), 0, math.random(1, 2))
	star.Position = UDim2.new(math.random(), 0, math.random(), 0)
	star.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	star.BorderSizePixel = 0
	star.BackgroundTransparency = math.random(0, 7) / 10
	star.ZIndex = 1
	star.Parent = StarsContainer

	table.insert(stars, {
		obj = star,
		speedX = (math.random(2, 6)) / 1000,
		speedY = (math.random(2, 6)) / 1000,
	})
end

task.spawn(function()
	while MainFrame.Parent do
		for _, s in ipairs(stars) do
			local pos = s.obj.Position
			local newX = pos.X.Scale + s.speedX
			local newY = pos.Y.Scale + s.speedY
			if newX > 1 then newX = 0 end
			if newY > 1 then newY = 0 end
			s.obj.Position = UDim2.new(newX, 0, newY, 0)
		end
		task.wait(0.03)
	end
end)

-- =========================
-- TÃTULO
-- =========================
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0, 250, 0, 30)
Title.Position = UDim2.new(0, 15, 0, 10)
Title.BackgroundTransparency = 1
Title.Text = "Astral X Hub"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 3
Title.Parent = MainFrame

-- =========================
-- ABAS (lado esquerdo)
-- =========================
local TabContainer = Instance.new("Frame")
TabContainer.Size = UDim2.new(0, 130, 0, 170)
TabContainer.Position = UDim2.new(0, 10, 0, 50)
TabContainer.BackgroundTransparency = 1
TabContainer.ZIndex = 3
TabContainer.Parent = MainFrame

local TabListLayout = Instance.new("UIListLayout")
TabListLayout.Padding = UDim.new(0, 8)
TabListLayout.Parent = TabContainer

local tabNames = {"Main", "ESP", "Local Player", "Teleporte"}
local tabButtons = {}

for _, name in ipairs(tabNames) do
	local tabBtn = Instance.new("TextButton")
	tabBtn.Size = UDim2.new(1, 0, 0, 36)
	tabBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
	tabBtn.BorderSizePixel = 0
	tabBtn.Text = name
	tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	tabBtn.Font = Enum.Font.Gotham
	tabBtn.TextSize = 14
	tabBtn.AutoButtonColor = false
	tabBtn.ZIndex = 3
	tabBtn.Parent = TabContainer

	local tabCorner = Instance.new("UICorner")
	tabCorner.CornerRadius = UDim.new(0, 8)
	tabCorner.Parent = tabBtn

	local tabStroke = Instance.new("UIStroke")
	tabStroke.Color = Color3.fromRGB(255, 255, 255)
	tabStroke.Thickness = 1.5
	tabStroke.Transparency = 1 -- invisÃ­vel atÃ© a aba ser selecionada
	tabStroke.Parent = tabBtn

	tabButtons[name] = tabBtn
end

-- =========================
-- PERFIL (abaixo das abas)
-- =========================
local ProfileFrame = Instance.new("Frame")
ProfileFrame.Size = UDim2.new(0, 130, 0, 50)
ProfileFrame.Position = UDim2.new(0, 10, 1, -60)
ProfileFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
ProfileFrame.BorderSizePixel = 0
ProfileFrame.ZIndex = 3
ProfileFrame.Parent = MainFrame

local profileCorner = Instance.new("UICorner")
profileCorner.CornerRadius = UDim.new(0, 8)
profileCorner.Parent = ProfileFrame

local profileStroke = Instance.new("UIStroke")
profileStroke.Color = Color3.fromRGB(255, 255, 255)
profileStroke.Thickness = 1.5
profileStroke.Parent = ProfileFrame

local AvatarImage = Instance.new("ImageLabel")
AvatarImage.Size = UDim2.new(0, 36, 0, 36)
AvatarImage.Position = UDim2.new(0, 7, 0, 7)
AvatarImage.BackgroundTransparency = 1
AvatarImage.ZIndex = 4
AvatarImage.Parent = ProfileFrame

local avatarCorner = Instance.new("UICorner")
avatarCorner.CornerRadius = UDim.new(1, 0)
avatarCorner.Parent = AvatarImage

local ok, content = pcall(function()
	return Players:GetUserThumbnailAsync(player.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
end)
if ok then
	AvatarImage.Image = content
end

local NameLabel = Instance.new("TextLabel")
NameLabel.Size = UDim2.new(0, 80, 0, 36)
NameLabel.Position = UDim2.new(0, 50, 0, 7)
NameLabel.BackgroundTransparency = 1
NameLabel.Text = player.Name
NameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
NameLabel.Font = Enum.Font.Gotham
NameLabel.TextSize = 13
NameLabel.TextXAlignment = Enum.TextXAlignment.Left
NameLabel.ZIndex = 4
NameLabel.Parent = ProfileFrame

-- =========================
-- ÃREA DE CONTEÃšDO (direita)
-- =========================
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(0, 380, 0, 280)
ContentFrame.Position = UDim2.new(0, 150, 0, 50)
ContentFrame.BackgroundTransparency = 1
ContentFrame.ZIndex = 3
ContentFrame.Parent = MainFrame

local pages = {}
for _, name in ipairs(tabNames) do
	local page = Instance.new("ScrollingFrame")
	page.Size = UDim2.new(1, 0, 1, 0)
	page.BackgroundTransparency = 1
	page.Visible = false
	page.ZIndex = 3
	page.ScrollBarThickness = 4
	page.AutomaticCanvasSize = Enum.AutomaticSize.Y -- cresce sozinha com o conteÃºdo
	page.CanvasSize = UDim2.new(0, 0, 0, 0)
	page.Parent = ContentFrame

	local pageLayout = Instance.new("UIListLayout")
	pageLayout.Padding = UDim.new(0, 8)
	pageLayout.SortOrder = Enum.SortOrder.LayoutOrder
	pageLayout.Parent = page

	local pagePadding = Instance.new("UIPadding")
	pagePadding.PaddingTop = UDim.new(0, 4)
	pagePadding.PaddingLeft = UDim.new(0, 4)
	pagePadding.PaddingRight = UDim.new(0, 4)
	pagePadding.Parent = page

	-- O conteÃºdo real de cada aba Ã© adicionado nos blocos marcados
	-- mais abaixo no script (procure pelos comentÃ¡rios "CONTEUDO DA ABA").

	pages[name] = page
end

-- =====================
-- CONTEUDO DA ABA MAIN
-- =====================
-- Adicione seus botÃµes/elementos aqui. Use pages["Main"] como Parent.
-- Exemplo:
-- local exampleBtn = Instance.new("TextButton")
-- exampleBtn.Size = UDim2.new(1, 0, 0, 32)
-- exampleBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
-- exampleBtn.Text = "Exemplo"
-- exampleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
-- exampleBtn.Parent = pages["Main"]


-- =====================
-- CONTEUDO DA ABA ESP
-- =====================
-- Adicione seus botÃµes/elementos aqui. Use pages["ESP"] como Parent.


-- =====================
-- CONTEUDO DA ABA LOCAL PLAYER
-- =====================
-- Adicione seus botÃµes/elementos aqui. Use pages["Local Player"] como Parent.


-- =====================
-- CONTEUDO DA ABA TELEPORTE
-- =====================
-- Adicione seus botÃµes/elementos aqui. Use pages["Teleporte"] como Parent.


local function selectTab(name)
	for tabName, btn in pairs(tabButtons) do
		local isSelected = (tabName == name)
		btn.BackgroundColor3 = isSelected and Color3.fromRGB(45, 45, 45) or Color3.fromRGB(20, 20, 20)
		local stroke = btn:FindFirstChildOfClass("UIStroke")
		if stroke then
			stroke.Transparency = isSelected and 0 or 1
		end
	end
	for pageName, page in pairs(pages) do
		page.Visible = (pageName == name)
	end
end

-- Som ao trocar de aba
local TabSound = Instance.new("Sound")
TabSound.SoundId = "rbxassetid://101301588142598"
TabSound.Volume = 0.5
TabSound.Parent = ScreenGui

for name, btn in pairs(tabButtons) do
	btn.MouseButton1Click:Connect(function()
		selectTab(name)
		TabSound:Play()
	end)
end

selectTab("Main")

-- =========================
-- ABRIR / FECHAR
-- =========================
local ToggleSound = Instance.new("Sound")
ToggleSound.SoundId = "rbxassetid://140207837688369"
ToggleSound.Volume = 0.5
ToggleSound.Parent = ScreenGui

local isOpen = false
ToggleButton.MouseButton1Click:Connect(function()
	isOpen = not isOpen
	MainFrame.Visible = isOpen
	ToggleSound:Play()
end)
